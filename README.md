# Bank Statement Reader

A simple Java program that parses raw bank transaction text files and converts
them into a clean, tab-separated format suitable for Google Sheets or Excel.

## Features
- Reads transaction data from a text file
- Skips non-transaction lines
- Extracts:
  - Date
  - Expense description
  - Transaction amount
- Outputs tab-separated values for easy spreadsheet import
